// Reels API Repository (Infrastructure)
// Port từ: client/src/reels/infrastructure/repositories/

import type { ReelsRepository } from '../../domain/repositories/ReelsRepository';
import apiClient from '../../../shared-kernel/infrastructure/api/client';

export function createReelsRepository(): ReelsRepository {
  return {
    // TODO: implement methods
  };
}
