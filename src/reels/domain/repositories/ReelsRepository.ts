// Reels Repository Interface
// Port từ: client/src/reels/domain/repositories/

import type { ReelsItem } from '../types/reels.types';

export interface ReelsRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<ReelsItem[]>;
  // getById(id: string | number): Promise<ReelsItem | null>;
}
