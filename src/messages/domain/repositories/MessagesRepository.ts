// Messages Repository Interface
// Port từ: client/src/messages/domain/repositories/

import type { MessagesItem } from '../types/messages.types';

export interface MessagesRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<MessagesItem[]>;
  // getById(id: string | number): Promise<MessagesItem | null>;
}
