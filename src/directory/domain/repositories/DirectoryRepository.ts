// Directory Repository Interface
// Port từ: client/src/directory/domain/repositories/

import type { DirectoryItem } from '../types/directory.types';

export interface DirectoryRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<DirectoryItem[]>;
  // getById(id: string | number): Promise<DirectoryItem | null>;
}
