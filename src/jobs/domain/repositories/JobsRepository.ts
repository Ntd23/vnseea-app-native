// Jobs Repository Interface
// Port từ: client/src/jobs/domain/repositories/

import type { JobsItem } from '../types/jobs.types';

export interface JobsRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<JobsItem[]>;
  // getById(id: string | number): Promise<JobsItem | null>;
}
