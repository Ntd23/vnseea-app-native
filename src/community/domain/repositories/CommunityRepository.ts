// Community Repository Interface
// Port từ: client/src/community/domain/repositories/

import type { CommunityItem } from '../types/community.types';

export interface CommunityRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<CommunityItem[]>;
  // getById(id: string | number): Promise<CommunityItem | null>;
}
