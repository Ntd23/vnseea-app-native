// Games API Repository (Infrastructure)
// Port từ: client/src/games/infrastructure/repositories/

import type { GamesRepository } from '../../domain/repositories/GamesRepository';
import apiClient from '../../../shared-kernel/infrastructure/api/client';

export function createGamesRepository(): GamesRepository {
  return {
    // TODO: implement methods
  };
}
