// Games Repository Interface
// Port từ: client/src/games/domain/repositories/

import type { GamesItem } from '../types/games.types';

export interface GamesRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<GamesItem[]>;
  // getById(id: string | number): Promise<GamesItem | null>;
}
