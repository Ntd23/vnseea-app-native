// Forum Repository Interface
// Port từ: client/src/forum/domain/repositories/

import type { ForumItem } from '../types/forum.types';

export interface ForumRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<ForumItem[]>;
  // getById(id: string | number): Promise<ForumItem | null>;
}
