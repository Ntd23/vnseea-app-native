// Memories Repository Interface
// Port từ: client/src/memories/domain/repositories/

import type { MemoriesItem } from '../types/memories.types';

export interface MemoriesRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<MemoriesItem[]>;
  // getById(id: string | number): Promise<MemoriesItem | null>;
}
