// Memories API Repository (Infrastructure)
// Port từ: client/src/memories/infrastructure/repositories/

import type { MemoriesRepository } from '../../domain/repositories/MemoriesRepository';
import apiClient from '../../../shared-kernel/infrastructure/api/client';

export function createMemoriesRepository(): MemoriesRepository {
  return {
    // TODO: implement methods
  };
}
