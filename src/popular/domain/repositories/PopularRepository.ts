// Popular Repository Interface
// Port từ: client/src/popular/domain/repositories/

import type { PopularItem } from '../types/popular.types';

export interface PopularRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<PopularItem[]>;
  // getById(id: string | number): Promise<PopularItem | null>;
}
