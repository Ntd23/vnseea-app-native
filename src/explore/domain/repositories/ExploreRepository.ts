// Explore Repository Interface
// Port từ: client/src/explore/domain/repositories/

import type { ExploreItem } from '../types/explore.types';

export interface ExploreRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<ExploreItem[]>;
  // getById(id: string | number): Promise<ExploreItem | null>;
}
