// Feed Repository Interface
// Port từ: client/src/feed/domain/repositories/

import type { FeedItem } from '../types/feed.types';

export interface FeedRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<FeedItem[]>;
  // getById(id: string | number): Promise<FeedItem | null>;
}
