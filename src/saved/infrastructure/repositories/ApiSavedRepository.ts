// Saved API Repository (Infrastructure)
// Port từ: client/src/saved/infrastructure/repositories/

import type { SavedRepository } from '../../domain/repositories/SavedRepository';
import apiClient from '../../../shared-kernel/infrastructure/api/client';

export function createSavedRepository(): SavedRepository {
  return {
    // TODO: implement methods
  };
}
