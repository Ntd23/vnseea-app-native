// Wallet Repository Interface
// Port từ: client/src/wallet/domain/repositories/

import type { EarningsMenuItem } from '../types/wallet.types';

export interface WalletRepository {
  // TODO: định nghĩa các methods từ API docs
  // getEarningsMenu(): Promise<EarningsMenuItem[]>;
}
