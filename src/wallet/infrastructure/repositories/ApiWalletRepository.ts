// Wallet API Repository (Infrastructure)
// Port từ: client/src/wallet/infrastructure/repositories/

import type { WalletRepository } from '../../domain/repositories/WalletRepository';
import apiClient from '../../../shared-kernel/infrastructure/api/client';

export function createWalletRepository(): WalletRepository {
  return {
    // TODO: implement methods
  };
}
