// Stories Repository Interface
// Port từ: client/src/stories/domain/repositories/

import type { StoriesItem } from '../types/stories.types';

export interface StoriesRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<StoriesItem[]>;
  // getById(id: string | number): Promise<StoriesItem | null>;
}
