// GoPro Repository Interface
// Port từ: client/src/go-pro/domain/repositories/

import type { GoProItem } from '../types/go-pro.types';

export interface GoProRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<GoProItem[]>;
  // getById(id: string | number): Promise<GoProItem | null>;
}
