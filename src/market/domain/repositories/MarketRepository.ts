// Market Repository Interface
// Port từ: client/src/market/domain/repositories/

import type { MarketItem } from '../types/market.types';

export interface MarketRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<MarketItem[]>;
  // getById(id: string | number): Promise<MarketItem | null>;
}
