// Market API Repository (Infrastructure)
// Port từ: client/src/market/infrastructure/repositories/

import type { MarketRepository } from '../../domain/repositories/MarketRepository';
import apiClient from '../../../shared-kernel/infrastructure/api/client';

export function createMarketRepository(): MarketRepository {
  return {
    // TODO: implement methods
  };
}
