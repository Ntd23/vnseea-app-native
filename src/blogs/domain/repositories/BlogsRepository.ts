// Blogs Repository Interface
// Port từ: client/src/blogs/domain/repositories/

import type { BlogsItem } from '../types/blogs.types';

export interface BlogsRepository {
  // TODO: định nghĩa các methods từ API docs
  // getAll(): Promise<BlogsItem[]>;
  // getById(id: string | number): Promise<BlogsItem | null>;
}
